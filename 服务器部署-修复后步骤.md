# 服务器部署 - 修复后的步骤

## ✅ 已修复的问题

1. TransactionType 类型问题（根因已修复）
2. Sample 接口类型定义
3. UserRole 类型断言
4. Import 路由类型错误

## 📋 在服务器上执行的步骤

### 1. 停止旧的 PM2 进程

```bash
cd ~/microtrace
pm2 stop microtrace
pm2 delete microtrace
pm2 save
```

### 2. 拉取最新代码

```bash
# 如果还没有添加gitee远程仓库
git remote add gitee https://gitee.com/bupoo/MicroTrace.git

# 拉取最新代码
git pull gitee main
```

### 3. 清理旧的构建文件

```bash
rm -rf .next
```

### 4. 重新构建

```bash
npm run build
```

### 5. 确认构建成功

```bash
ls -la .next/BUILD_ID
```

如果看到 `BUILD_ID` 文件，说明构建成功。

### 6. 启动应用（必须先build）

```bash
pm2 start npm --name microtrace -- start
pm2 save
pm2 logs microtrace
```

### 7. 查看状态

```bash
pm2 status
```

## ⚠️ 重要提示

1. **不要在 build 失败时运行 `npm start`**（会失败，因为缺少 BUILD_ID）
2. **必须先 build 成功，再启动应用**
3. useEffect 的 warning 可以先忽略，不影响运行

## 🎯 如果还有问题

如果构建失败，请把完整的错误信息发给我。

