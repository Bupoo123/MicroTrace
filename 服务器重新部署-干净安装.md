# 服务器重新部署 - 干净安装方案

## 🎯 推荐方案：删除后重新克隆（最干净）

这样可以避免之前的构建错误、依赖问题等。

### 步骤1：删除旧目录

```bash
cd ~
rm -rf microtrace
```

### 步骤2：重新克隆

```bash
git clone https://gitee.com/bupoo/MicroTrace.git microtrace
cd microtrace
```

### 步骤3：运行部署脚本

```bash
chmod +x deploy-server.sh
./deploy-server.sh
```

部署脚本会自动完成：
- 安装 Node.js（如果需要）
- 安装所有依赖
- 初始化数据库
- 构建应用
- 启动 PM2

---

## 🔄 方案二：保留目录，只更新代码（如果不想删除）

如果不想删除，可以更新现有代码：

### 步骤1：停止 PM2

```bash
cd ~/microtrace
pm2 stop microtrace
pm2 delete microtrace
pm2 save
```

### 步骤2：清理构建文件

```bash
rm -rf .next
rm -rf node_modules
```

### 步骤3：拉取最新代码

```bash
git remote add gitee https://gitee.com/bupoo/MicroTrace.git
git pull gitee main
```

### 步骤4：重新安装和构建

```bash
npm install
npm run build
```

### 步骤5：启动应用

```bash
pm2 start npm --name microtrace -- start
pm2 save
pm2 logs microtrace
```

---

## ✅ 推荐使用方案一（删除重新克隆）

因为：
- 更干净，避免之前的错误残留
- 部署脚本会自动处理所有步骤
- 更简单，不容易出错

---

## 📝 执行后验证

部署完成后：

1. 检查 PM2 状态：
   ```bash
   pm2 status
   ```

2. 查看日志：
   ```bash
   pm2 logs microtrace
   ```

3. 访问应用：
   - http://121.36.255.207:5005
   - 使用 `admin` / `123456` 登录

